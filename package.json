{
  "name": "salton-sea",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:css": "sass --load-path=node_modules src/assets/scss/main.scss src/assets/css/main.css --no-source-map",
    "watch:css": "sass --load-path=node_modules --watch src/assets/scss/main.scss src/assets/css/main.css",
    "clean": "rimraf _site",
    "build": "npm run build:css && npm run clean && eleventy",
    "dev": "concurrently -k -n SASS,11TY \"npm:watch:css\" \"eleventy --serve\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "bootstrap": "^5.3.7",
    "concurrently": "^9.2.0",
    "rimraf": "^6.0.1",
    "sass": "^1.90.0"
  },
  "dependencies": {
    "bootstrap-icons": "^1.13.1"
  }
}
